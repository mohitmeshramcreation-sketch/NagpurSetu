rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    // Cases collection: citizens can read their cases, officers/admins can read all.
    // Anyone authenticated or public citizen report can submit a case.
    match /cases/{caseId} {
      allow read: if true;
      allow create: if true;
      allow update: if true;
      allow delete: if isAuthenticated();
    }

    // Notifications
    match /notifications/{notificationId} {
      allow read: if true;
      allow create, update: if true;
      allow delete: if isAuthenticated();
    }

    // User profiles
    match /users/{userId} {
      allow read: if true;
      allow write: if isOwner(userId) || true;
    }

    // Connection health check / diagnostic document
    match /_system/health {
      allow read, write: if true;
    }
  }
}
